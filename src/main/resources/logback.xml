<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="jsonstdout" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
            <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                <prettyPrint>false</prettyPrint>
            </jsonFormatter>
            <timestampFormat>yyyy-MM-dd' 'HH:mm:ss.SSS</timestampFormat>
            <appendLineSeparator>true</appendLineSeparator>
        </layout>
    </appender>

    <!--
        AUDIT logger — security-relevant events (D-13 / SR-8).
        All events routed here must use scrubbed values only (see LogScrubber).
        Raw cookies, authorization headers, wallId, and access tokens must never
        appear in messages sent to this appender.
    -->
    <logger name="AUDIT" level="info" additivity="false">
        <appender-ref ref="jsonstdout"/>
    </logger>

    <root level="info">
        <appender-ref ref="jsonstdout"/>
    </root>
</configuration>