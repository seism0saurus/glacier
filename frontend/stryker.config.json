{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Stryker mutation testing for the Glacier frontend (SR-TEST / frontend counterpart to the backend PIT 'mutation' profile). Scoped to small, pure-logic, security-relevant units that are well covered by unit + fast-check property specs, so the run stays fast and the score high. Run with: npm run mutation",
  "packageManager": "npm",
  "testRunner": "karma",
  "karma": {
    "projectType": "angular-cli",
    "configFile": "karma.conf.js",
    "config": {
      "browsers": ["ChromeHeadless"]
    }
  },
  "coverageAnalysis": "all",
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "mutate": [
    "src/app/util/hashtag.ts",
    "src/app/util/safe-storage.ts",
    "src/app/share/pipes/safe-url.pipe.ts"
  ],
  "concurrency": 2,
  "timeoutMS": 60000,
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 70
  }
}
