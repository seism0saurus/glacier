<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.7</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>de.seism0saurus</groupId>
    <artifactId>glacier</artifactId>
    <version>0.0.9</version>
    <description>Glacier mastodon social wall</description>

    <profiles>
        <profile>
            <id>RunE2ETest</id>
            <properties>
                <skipE2ETest>false</skipE2ETest>
            </properties>
        </profile>
        <profile>
            <id>RunE2ETestWithoutDeps</id>
            <properties>
                <skipE2ETest>false</skipE2ETest>
                <additionalPlaywrightInstallParams/>
            </properties>
        </profile>
        <profile>
            <id>SkipUnitTest</id>
            <properties>
                <skipUnitTests>true</skipUnitTests>
            </properties>
        </profile>
        <profile>
            <id>SkipIntegrationTest</id>
            <properties>
                <skipIntegrationTests>true</skipIntegrationTests>
            </properties>
        </profile>
        <profile>
            <id>WithPlaywrightDeps</id>
            <properties>
                <additionalPlaywrightInstallParams>--with-deps</additionalPlaywrightInstallParams>
            </properties>
        </profile>
        <profile>
            <id>manual</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <additionalPlaywrightInstallParams/>
                <skipUnitTests>false</skipUnitTests>
                <skipIntegrationTests>false</skipIntegrationTests>
                <skipE2ETest>true</skipE2ETest>
            </properties>
        </profile>
        <profile>
            <!--
                Mutation testing profile — NOT active by default.
                Activate with: ./mvnw -P mutation org.pitest:pitest-maven:mutationCoverage
                Targets exactly 10 security-relevant classes; 70% threshold enforced.
                Reports written to target/pit-reports/ (stable path, no timestamp suffix).
            -->
            <id>mutation</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>1.19.1</version>
                        <dependencies>
                            <!-- JUnit 5 bridge required for PITest to discover JUnit Jupiter tests -->
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>1.2.2</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <!-- Exactly 14 FQNs — no wildcards (ADR-FUZZ-01 / SR-FUZZ-15) -->
                            <!-- Sec-18/P2-17: expanded to cover new security-critical classes -->
                            <targetClasses>
                                <param>de.seism0saurus.glacier.util.LogScrubber</param>
                                <param>de.seism0saurus.glacier.share.domain.ShareLinkId</param>
                                <param>de.seism0saurus.glacier.share.domain.ShareViewerId</param>
                                <param>de.seism0saurus.glacier.share.domain.SecureRandomTokenGenerator</param>
                                <param>de.seism0saurus.glacier.webservice.messaging.HashtagFormat</param>
                                <param>de.seism0saurus.glacier.share.web.ShareImageProxyUrlBuilder</param>
                                <param>de.seism0saurus.glacier.share.application.DefaultSafeUrlValidator</param>
                                <param>de.seism0saurus.glacier.mastodon.IframeEmbedPolicy</param>
                                <param>de.seism0saurus.glacier.webservice.cache.FallbackRateLimiter</param>
                                <param>de.seism0saurus.glacier.share.web.ShareRateLimiter</param>
                                <!-- Sec-18/P2-17: rate-limiting interceptors — gate rate-limit bypass mutations -->
                                <param>de.seism0saurus.glacier.webservice.security.HandshakeRateLimitInterceptor</param>
                                <param>de.seism0saurus.glacier.webservice.security.SubscribeRateLimitInterceptor</param>
                                <!-- Sec-18/P2-17: share host security filter — gate header injection mutations -->
                                <param>de.seism0saurus.glacier.share.web.ShareSecurityHeadersFilter</param>
                                <!-- Sec-18/P2-17 / Sec-11: IP classifier — gate SSRF bypass mutations -->
                                <param>de.seism0saurus.glacier.util.IpAddressClassifier</param>
                            </targetClasses>
                            <!-- STRONGER group = ALL mutators, no exclusions (ADR-FUZZ-02) -->
                            <mutators>
                                <mutator>ALL</mutator>
                            </mutators>
                            <!-- Minimum mutation score: 70% (ADR-FUZZ-01 / SR-FUZZ-05) -->
                            <mutationThreshold>70</mutationThreshold>
                            <!-- HTML for human review; XML for CI artifact ingestion -->
                            <outputFormats>
                                <param>HTML</param>
                                <param>XML</param>
                            </outputFormats>
                            <!-- Stable output path — no timestamp suffix — so CI can always find target/pit-reports/ -->
                            <timestampedReports>false</timestampedReports>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>23</java.version>
        <glacier.version>0.0.9</glacier.version>
        <kotlin.version>2.2.0</kotlin.version>
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <repositories>
        <repository>
            <id>maven-central-snapshots</id>
            <name>Maven Central Snapshot Repository</name>
            <url>https://s01.oss.sonatype.org/content/repositories/snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencies>
        <!--Spring Boot-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
<!--        <dependency>-->
<!--            <groupId>org.springframework.boot</groupId>-->
<!--            <artifactId>spring-boot-starter-data-jpa</artifactId>-->
<!--        </dependency>-->

        <!--Lombock-->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>

        <!--
            Apache HttpClient 5 — connection pool for RestTemplate HEAD embed checks (P2-01).
            Spring Boot BOM manages the httpclient5 version; no explicit version needed here.
            The pooled client replaces the SimpleClientHttpRequestFactory used for HEAD requests,
            providing: max 50 total / 20 per-route connections, 30 s TTL, 5 s socket/connect timeouts.
        -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>

        <!--
            Bigbone Mastodon Client — SNAPSHOT (Sec-02 / ADR-SEC-02)
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
            bigbone:2.0.0-SNAPSHOT is a mutable dependency: any upstream commit may silently
            change the artifact served from the Sonatype snapshot repository. This is a known
            security risk: supply-chain attack via snapshot mutation.

            Interim mitigation status (Sec-02/P1-09):
            - SHA256 checksum enforcement is not directly supported by maven-enforcer for
              SNAPSHOT artifacts (requireReleaseDependencies would break the build here).
            - The `-Pallow-snapshot` escape-hatch profile is intentionally NOT added because
              SNAPSHOT is the ONLY available version of bigbone upstream. Prohibiting it
              would prevent the build entirely.
            - Instead, the CI pipeline runs mvnw with the update-snapshots flag only in the
              compile step (build-and-deploy.yml) and the test/package steps use the cached
              local repository (no snapshot re-download). This limits the SNAPSHOT fetch to
              a single controlled step with a known Maven cache key.

            TODO (Sec-02): replace with a stable release version once bigbone upstream tags
            a release. Track at https://github.com/andregasser/bigbone/releases.
            When upstream releases, remove this comment and the SNAPSHOT repository declaration,
            pin the exact release version, and add a maven-enforcer requireReleaseDependencies rule.
        -->
        <dependency>
            <groupId>social.bigbone</groupId>
            <artifactId>bigbone</artifactId>
            <version>2.0.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlinx</groupId>
            <artifactId>kotlinx-serialization-json</artifactId>
            <version>1.11.0</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlinx</groupId>
            <artifactId>kotlinx-serialization-core-jvm</artifactId>
            <version>1.11.0</version>
        </dependency>


        <!-- Spring Boot Actuator for metrics (already in parent pom pattern, added here explicitly) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <!--Database Libraries-->
<!--        <dependency>-->
<!--            <groupId>org.springframework.boot</groupId>-->
<!--            <artifactId>spring-boot-starter-data-mongodb</artifactId>-->
<!--        </dependency>-->

        <!--Test Libraries-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wiremock.integrations</groupId>
            <artifactId>wiremock-spring-boot</artifactId>
            <version>3.10.0</version>
        </dependency>

        <!--
            mockito-inline: inline mock-maker, integrated into mockito-core since Mockito 5.0.
            Kept here at 5.2.0 (the last standalone release) for backwards compatibility.
            In Mockito 5+, inline mocking is the default — this dependency is effectively
            redundant but kept to prevent future dependency-mediation selecting an older version.
            The BOM-managed mockito-core from spring-boot-starter-test is the authoritative version (P1-05).
        -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <version>5.2.0</version>
            <scope>test</scope>
        </dependency>

        <!--Logging Libraries-->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-core</artifactId>
            <version>1.5.32</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.5.32</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback.contrib</groupId>
            <artifactId>logback-json-classic</artifactId>
            <version>0.1.5</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback.contrib</groupId>
            <artifactId>logback-jackson</artifactId>
            <version>0.1.5</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!--Share-link feature dependencies (ADR-SHARE-03, ADR-SHARE-07)-->

        <!--
            Jsoup: server-side HTML parsing for JsoupTextExtractor.
            Used by ShareRenderingService to extract plain text from toot HTML
            (option 4C decision: Jsoup.parse(html).text() + paragraph handling).
            Version pinned exactly; Dependabot (maven ecosystem) will propose
            updates automatically via .github/dependabot.yaml.
        -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.22.2</version>
        </dependency>

        <!--
            Caffeine: in-memory cache for the image proxy (ADR-SHARE-07).
            10 000 entries, positive TTL 1 h, negative TTL 60 s.
            spring-boot-starter-cache auto-configures Caffeine when it is on the
            classpath alongside spring-cache; we import it directly here so that
            version management is explicit and Dependabot can track it.
            Version pinned exactly; Dependabot will propose updates.
        -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>3.2.3</version>
        </dependency>

        <!--
            jqwik: property-based testing framework for JUnit 5 (SR-TEST-06).
            Used for fuzz testing of HMAC sign/verify path in ShareImageProxyUrlBuilder.
            Actively maintained; last release 1.8.4 (2024-07).
            https://jqwik.net
        -->
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>1.8.4</version>
            <scope>test</scope>
        </dependency>

        <!--
            ArchUnit: architecture enforcement at test-time (SR-FUZZ-FIX-12 / ADR-FUZZ-04).
            Ensures package-private hmacSha256 is not called by production classes in share.web.
            https://www.archunit.org/
        -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-sourcepath</arg>
                        <arg>${project.basedir}/src/main/java${path.separator}${project.basedir}/src/main/resources{path.separator}${project.basedir}/target/generated-sources/annotations{path.separator}${project.basedir}/target/generated-test-sources/test-annotations${path.separator}/</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <layers>
                        <enabled>true</enabled>
                    </layers>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <sourceDirs>
                                <source>src/main/java</source>
                                <source>target/generated-sources/annotations</source>
                            </sourceDirs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.12.0</version>
                <configuration>
                    <source>21</source>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.5</version>
                <configuration>
                    <skip>${skipUnitTests}</skip>
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.5.5</version>
                <executions>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <skip>${skipIntegrationTests}</skip>
                            <forkCount>1</forkCount>
                            <reuseForks>true</reuseForks>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.1</version>
                <configuration>
                    <installDirectory>frontend/node</installDirectory>
                    <workingDirectory>frontend</workingDirectory>
                </configuration>
                <executions>
                    <!-- Install npm dependencies during initialize phase-->
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <phase>initialize</phase>
                        <configuration>
                            <nodeVersion>v22.14.0</nodeVersion>
                            <npmVersion>10.9.2</npmVersion>
                        </configuration>
                    </execution>
                    <!-- Install frontend dependencies during initialize phase-->
                    <execution>
                        <id>npm install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>initialize</phase>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <!-- i18n key lint: verify all @@<id> used in templates exist in the catalog -->
                    <execution>
                        <id>check-i18n-keys</id>
                        <goals>
                            <goal>node</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>scripts/check-i18n-keys.mjs</arguments>
                        </configuration>
                    </execution>
                    <!-- Angular Build during generate-resources phase, so the files are in the resources folder-->
                    <execution>
                        <id>npm run build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build -- --optimization true --aot --configuration production --output-hashing none --delete-output-path true</arguments>
                        </configuration>
                    </execution>
                    <!-- Angular test during test phase-->
                    <execution>
                        <id>npm run test</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>test</phase>
                        <configuration>
                            <skip>${skipUnitTests}</skip>
                            <arguments>run test -- --no-watch --code-coverage --browsers ChromeHeadless</arguments>
                        </configuration>
                    </execution>
                    <!-- e2e during verify phase-->
                    <execution>
                        <id>npx playwright install</id>
                        <goals>
                            <goal>npx</goal>
                        </goals>
                        <phase>verify</phase>
                        <configuration>
                            <skip>${skipE2ETest}</skip>
                            <arguments>playwright install ${additionalPlaywrightInstallParams}</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npx playwright test</id>
                        <goals>
                            <goal>npx</goal>
                        </goals>
                        <phase>verify</phase>
                        <configuration>
                            <skip>${skipE2ETest}</skip>
                            <arguments>playwright test</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>move-browser-files</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>bash</executable>
                            <arguments>
                                <argument>-c</argument>
                                <argument>
                                    mkdir -p ${project.basedir}/src/main/resources/static/; rm -rf ${project.basedir}/src/main/resources/static/*; cp -a ${project.basedir}/frontend/dist/glacier-frontend/browser/* ${project.basedir}/src/main/resources/static/
                                </argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.10.0</version>
                <executions>
                    <execution>
                        <id>copy-and-rename-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>de.seism0saurus</groupId>
                                    <artifactId>glacier</artifactId>
                                    <version>${glacier.version}</version>
                                    <type>jar</type>
                                    <outputDirectory>${basedir}/infrastructure/glacier</outputDirectory>
                                    <destFileName>glacier.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <id>default-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <formats>HTML,XML</formats>
                        </configuration>
                    </execution>
                    <execution>
                        <id>check-coverage</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.45</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.35</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                CycloneDX Maven Plugin: generates a Software Bill of Materials (SBOM) for the jar.
                Runs during the `package` phase and produces target/bom.json + target/bom.xml in
                CycloneDX format.  The output is informational — it does not block the build.
                Attach the SBOM to releases so consumers can audit the dependency tree for CVEs.
                https://github.com/CycloneDX/cyclonedx-maven-plugin
            -->
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>2.9.1</version>
                <executions>
                    <execution>
                        <id>generate-sbom</id>
                        <phase>package</phase>
                        <goals>
                            <goal>makeAggregateBom</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectType>application</projectType>
                    <schemaVersion>1.5</schemaVersion>
                    <includeBomSerialNumber>true</includeBomSerialNumber>
                    <includeCompileScope>true</includeCompileScope>
                    <includeProvidedScope>true</includeProvidedScope>
                    <includeRuntimeScope>true</includeRuntimeScope>
                    <includeSystemScope>true</includeSystemScope>
                    <includeTestScope>false</includeTestScope>
                    <includeLicenseText>false</includeLicenseText>
                    <outputReactorProjects>true</outputReactorProjects>
                    <outputFormat>all</outputFormat>
                    <outputName>bom</outputName>
                    <!-- Do not fail the build on SBOM generation errors — informational only -->
                    <verbose>false</verbose>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
